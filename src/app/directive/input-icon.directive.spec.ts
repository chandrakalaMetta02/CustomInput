import { InputIconDirective } from './input-icon.directive';

describe('InputIconDirective', () => {
  it('should create an instance', () => {
    const directive = new InputIconDirective();
    expect(directive).toBeTruthy();
  });
});
